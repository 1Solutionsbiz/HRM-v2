node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# build output
/dist
*.tsbuildinfo

# Local-dev copy of UPLOADS_DIR's default ('./uploads') - real
# document/avatar/letter files should never land in version control.
/uploads
